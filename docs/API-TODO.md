# ReelForge API TODO

旧增量计划已废弃，不再作为实施依据。

当前接口契约以运行时 Swagger UI 为准：

```text
http://localhost:3005/docs
```

重新实现内容生成链路时，需要重新补一份计划，至少明确：

1. 入口接口和请求 schema。
2. 是否复用公众号文章读取接口。
3. 是否走现有 `topic-queue`，还是新增独立队列。
4. 结果结构、失败错误码和前端展示边界。
5. 可复现的端到端验收用例。
